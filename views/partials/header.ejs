<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>Order Helper</title>
    <link rel="stylesheet" href="/css/bootstrap.min.css">
    <link rel="stylesheet" href="/font-awesome/css/font-awesome.min.css">
    <link rel="stylesheet" href="/css/index.css">
    <script src="https://ajax.googleapis.com/ajax/libs/jquery/3.2.1/jquery.min.js"></script>
    <script defer src="/js/popper.min.js" charset="utf-8"></script>
    <script defer src="/js/bootstrap.min.js" charset="utf-8"></script>
    <script type="text/javascript" src="js/main.js"></script>

  </head>
  <body>

    <div class="row navBar">
        <nav class="navbar navbar-toggleable-md navbar-light bg-faded">
            <button class="navbar-toggler navbar-toggler-right" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation" style="margin-bottom: 10px;">
              <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="navbarSupportedContent">
              <form class="form-inline my-2 my-lg-0">
                <input class="form-control mr-sm-2" type="text" placeholder="Business name">
                <button class="btn btn-outline-success my-2 my-sm-0" type="submit">Search</button>
              </form>
              <ul class="navbar-nav mr-auto">
                <li class="nav-item active">
                  <a class="nav-link" href="/">Home <span class="sr-only">(current)</span></a>
                </li>
              </ul>
            </div>
          </nav>
    <%# Sign in & Sign up%>
            <ul class="ml-auto"style="margin-right: 20px; margin-top: 10px">
             <% if (currentUser) { %>
               <li class="nav-item" style="display: inline-block">
                <span class="navbar-text"><strong>Hello <%= currentUser.username %></strong></span>
               </li>
               <li class="nav-item ml-2" style="display: inline-block">
                 <form class="form-inline" action="/session?_method=DELETE" method="POST">
                   <input class="btn btn-primary" type="submit" value="Sign Out">
                 </form>
               </li>
             <% } else { %>
              <li class="nav-item" style="display: inline-block">
               <form class="form-inline" action="/session/new"method="GET">
                 <input class="btn btn-outline-primary"type="submit" value="Sign In">
               </form>
              </li>
              <li class="nav-item" style="display: inline-block">
                <form class="form-inline" action="/users/new" method="GET">
                  <input class="btn btn-outline-primary" type="submit" value="Sign Up">
                </form>
              </li>
              <% } %>
            </ul>

      </div>

      <div class="container-fluid mainBackground">
       <% let message; while (message = flash.shift()) { %>
         <div class="alert alert-<%= message.type %> alert-dismissible fade show">
           <%= message.message %>
           <button type="button" class="close" data-dismiss="alert">
             <span aria-hidden="true">&times;</span>
           </button>
         </div>
       <% } %>
